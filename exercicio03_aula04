distancia = float(input('Digite a distância entre as cidades em km: '))
tempoViagem = float(input('Digite o tempo de viagem em horas: '))
velocidadeMedia = distancia / tempoViagem


print('A velocidade média do carro é: ', velocidadeMedia, 'km por hora')